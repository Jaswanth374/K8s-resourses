apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: NodePort
  selector:
    project: roboshop
    env: dev
    component: frontend
  ports:
    port: 80        # service port
    targetPort: 80  # container port
    nodeport: 32141