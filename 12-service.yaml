apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: roboshop
spec:
  selector:
    project: roboshop
    env: dev
    component: frontend
  ports:
  - protocol: TCP
    port: 80        # service port
    targetPort: 80  # container port